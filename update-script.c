#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_CMD 512
// error code 1 is caused by a missing dependency
int main() 
{
system("cd ~/.config/hypr");
  // check the current version in the hyprland config and match it to the (current one)

  
  return 0;
}
